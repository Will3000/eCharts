extends ../layout

block content
  ul(id="tables")
  each val in tables
    li
      a(href="http://localhost:3000/tables/" + val.id)= val.name
      div.box
        iframe(src="http://localhost:3000/tables/" + val.id, width = "500px", height = "500px")
